spring:
  application:
    name: api-sidep-grupo-matricula
  cloud:
    config:
      failFast: true
      uri: http://dockerdesa.mineduc.cl/properties-centralizados/
      server:
        native:
          searchLocations: classpath:/